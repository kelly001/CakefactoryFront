import React, { Component } from 'react'
import Form from '../UI/Form'


class Cakes  extends Component  {
  constructor(props) {
    super(props)
  }

  componentDidMount() {
  }

  render () {
    return (
      <Form />
    )
  }
}